/**
 * Shared hairline pill language for owner / nav / secondary actions.
 * Do not use on native form controls (inputs, selects) or ConfirmActionDialog.
 */
export const chipButton =
  "inline-flex items-center justify-center rounded-full border border-zinc-300 bg-white px-4 py-1.5 text-xs font-medium text-zinc-700 hover:bg-zinc-50 hover:border-zinc-500";

export const chipButtonPrimary =
  "inline-flex items-center justify-center rounded-full bg-zinc-900 px-4 py-2 text-xs font-medium text-white hover:bg-zinc-800";

/** Minimum 44px touch target for icon/avatar/hamburger controls. */
export const hitTarget = "min-h-11 min-w-11";
